
---

deployment:

    tasks:
    - export DEPLOYPATH=/home/uxuezerb/sites/acertarNumero.uxue.zerbitzaria.net
    - rm -Rf $DEPLOYPATH/*
    - cp -Rf css $DEPLOYPATH
    - cp -Rf images $DEPLOYPATH
    - cp -Rf js  $DEPLOYPATH
    - cp -Rf AZTERKETA.html  $DEPLOYPATH